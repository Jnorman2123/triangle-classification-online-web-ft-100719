class Triangle
  # write code here
  attr_accessor :a_length, :b_length, :c_length
  def initialize(a_length, b_length, c_length)
    @a_length = a_length
    @b_length = b_length
    @c_length = c_length
  end

  def kind

  end
end
